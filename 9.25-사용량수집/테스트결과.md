# 테스트 결과

## 테스트 환경

- 기준: `origin/develop` `a276ad4da6c90790c763984546ca6bbd497bd1ba`
- Python: 3.14
- cgroup: v2
- Docker: WSL CLI/daemon 미연결

## 결과

```text
Runner 전체:  211 PASS, 0 FAIL, 40 SKIP
Backend 전체:   3 PASS, 0 FAIL,  0 SKIP
compileall:      PASS
합계:          214 PASS, 0 FAIL, 40 SKIP
```

SKIP 40개는 Docker 및 eBPF 활성화가 필요한 기존 조건부 통합 테스트다.

검증 항목:

- Memory sampler 정상 동작 및 100ms interval
- `elapsed_ms` 증가
- `memory.current` 정상/실패 읽기
- 음수가 아닌 `memory_bytes`
- final sample
- 읽기 실패/예외가 실행 결과를 망가뜨리지 않음
- monotonic 기반 `wall_time_ms`
- TIME_LIMIT, MEMORY_LIMIT, PIDS_LIMIT, FILESYSTEM_LIMIT
- SIGSEGV/TIME_LIMIT 경쟁 처리
- Runner API 계약
- Backend API 및 SQLite JSON DB round-trip
- 기존 CPU sampler 회귀 테스트(코드 변경 없음)

## 실제 Memory + Wall Time

`test_programs/memory_steps.c`를 실행하고 동일 sampler로 WSL cgroup v2의
`memory.current`를 읽었다. 공유 `init.scope` 값이라 절대 사용량에는 다른
프로세스가 포함되지만, 8MiB 단계 증가와 시간축은 실제 값이다.

```text
Wall Time: 3072 ms

102ms  → 819.1 MB
306ms  → 827.1 MB
602ms  → 835.1 MB
806ms  → 843.1 MB
1101ms → 851.1 MB
1305ms → 859.1 MB
1601ms → 867.4 MB
1907ms → 875.4 MB
3072ms → 810.9 MB (해제 후 final sample)
```

마지막 sample의 `elapsed_ms`와 `wall_time_ms`는 모두 3072ms였다.

## 실제 Wall Time

`test_programs/wall_time_sleep.c`의 `sleep(2)` 측정 결과:

```text
Wall Time: 2001 ms
허용 범위: 1800ms ~ 2500ms
결과: PASS
```

## Docker 테스트

```text
NOT TESTED / SKIP
```

`docker version`, `docker info`, `docker ps` 모두 WSL Docker CLI 미연결로
실행할 수 없었다. 따라서 CodeGuard 실제 execution cgroup에서의 C 프로그램
통합 테스트는 수행하지 않았으며, 이를 성공으로 간주하지 않는다.
